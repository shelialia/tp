package seedu.guestnote.model.guest;

/**
 * Represents a Check-Out date in the guestnote book.
 */
public class CheckOut extends StayDuration {
    public CheckOut(String date) {
        super(date);
    }
}
