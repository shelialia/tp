package seedu.guestnote.testutil;

import seedu.guestnote.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_GUEST = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_GUEST = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_GUEST = Index.fromOneBased(3);
    public static final Index INDEX_EIGHTH_GUEST = Index.fromOneBased(8);
}
